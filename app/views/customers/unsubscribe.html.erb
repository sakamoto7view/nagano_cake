<%= render "layouts/header" %>


<h3 class="text-center">
  <strong>本当に退会しますか？</strong><br>
  <br></br>
退会すると、会員登録情報や<br>
これまでの購入履歴が閲覧できなくなります。<br>
退会する場合は、「退会する」をクリックしてください。
</h3>

<div class="container">
  <div class="row"> 
    <div class="col-lg-4">
    </div>
    <div class="col-lg-2">
      <div class="mypage">
        <h4 class="text-center">
          <%= link_to customer_path(@customer.id) do %>
            <%= "退会しない" %>
          <% end %>
        </h4>
      </div>
    </div>
    <div class="col-lg-2">
      <div class="withdrawl">
        <h4 class="text-center">
          <%= link_to "退会する", "/customers/:id/withdrawl", method: :patch, data: { confirm: "本当に退会してもよろしいですか？"}  %>
          <%#= form_with model:@customer, withdrawl_customer_path(@customer.id), method: :patch, local:true do |f| %>
				    <%#= f.submit '退会する' %>
			    <%# end %>
          
        </h4>
      </div>
    </div>
    <div class="col-lg-4">
    </div>
  </div>
</div>